# Anthropic (Primary LLM)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI (Image Generation)
# Required if using IMAGE_GENERATOR=gpt-5
OPENAI_API_KEY=your_openai_api_key_here

# Google Gemini (Image Generation - Recommended)
# Required if using IMAGE_GENERATOR=gemini
# Get your API key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Supabase (Image Storage - optional, defaults to local)
SUPABASE_URL=your_supabase_project_url_here
SUPABASE_KEY=your_supabase_anon_key_here
SUPABASE_BUCKET=design-images

# Embedding Configuration (Local HuggingFace - no API key required)
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
EMBEDDING_DIMENSION=384

# Image Generation
IMAGE_GENERATOR=gemini  # Options: gemini, gpt-5, placeholder
# Use gemini for real image generation with Google Gemini (Recommended - supports text-to-image AND image editing)
# Use gpt-5 for real image generation with OpenAI
# Use placeholder for development/testing (no API calls)

# Image Storage
IMAGE_STORAGE=supabase  # Options: local, supabase
# Use supabase for cloud storage (recommended - required for image editing and cross-room inspiration)
# Use local for filesystem storage (development only - image editing features won't work)

# App Configuration
ENVIRONMENT=development
LOG_LEVEL=INFO
CHROMA_DB_PATH=./chroma_db
DATA_STORAGE_PATH=./data
